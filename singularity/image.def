Bootstrap: docker
From: ubuntu:22.04

%files
    /volatile/aurelien_stumpf_mascles/project/code /code

%post
    apt-get update && apt-get install -y python3-venv && apt-get install -y python3-pip
    pip install -r /code/requirements.txt

%runscript
    python3 /code/computation.py $1 $2 $3 $4

